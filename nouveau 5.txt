Remplace cette partie :
jsif (valIntro === "Un syndicat mixte" && condition === "Un syndicat mixte") {

if (valIntro === "un syndicat mixte" && condition === "un syndicat mixte") {


if (valIntro === condition) {


<script>
document.addEventListener("DOMContentLoaded", function () {
  const normalize = s => String(s || "").trim().toLowerCase();
  const valIntro = normalize(localStorage.getItem("q2"));
  console.log("Valeur stockée (normalisée) :", valIntro);

  document.querySelectorAll(".conditional").forEach(block => {
    const condition = normalize(block.dataset.condition);
    console.log("Condition attendue :", condition);

    if (valIntro === condition) {
      block.style.display = "block";
      block.classList.add("visible");
    } else {
      block.style.display = "none";
      block.classList.remove("visible");
    }
  });
});
</script>
